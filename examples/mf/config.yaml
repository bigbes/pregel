# One master and three workers, each the read-write leader of a replicaset of
# its own. The master loads the ratings and drives the epochs; the workers need
# app_cfg all the same, because rank, epochs, the learning rate, its decay and
# lambda are read by the compute function through the worker context.

credentials:
  users:
    pregel:
      password: 'secret'
      privileges:
        - permissions: [execute]
          lua_call:
            - pregel.worker.deliver
            - pregel.worker.deliver_batch
            - pregel.worker.wait
            - pregel.master.deliver

wal:
  mode: none

groups:
  pregel:
    replicasets:
      master:
        instances:
          master:
            iproto:
              listen:
                - uri: '127.0.0.1:3301'
            roles: [pregel.roles.master]
            roles_cfg:
              pregel.roles.master:
                name: mf
                app: app
                autostart: true
                user: pregel
                password: 'secret'
                workers:
                  - '127.0.0.1:3302'
                  - '127.0.0.1:3303'
                  - '127.0.0.1:3304'
                app_cfg: &app_cfg
                  # Paths are relative to examples/mf/ and resolved by the app
                  # module: an instance's own working directory is under
                  # var/lib and is no use as a base.
                  train: '../../test/fixtures/ratings/train.avro'
                  # Not read by the job. examples/mf/evaluate.lua takes this
                  # path as its argument, and naming it here is what keeps the
                  # two halves of the example pointing at one dataset.
                  test: '../../test/fixtures/ratings/test.avro'
                  rank: 3
                  # 150 epochs at decay 0.99 rather than 30 at 0.98. The
                  # schedule here is Jacobi-style -- each side steps from the
                  # neighbours as they were an epoch ago -- so it converges
                  # more slowly per epoch than a sequential SGD, and 30 epochs
                  # left the fit less than halfway from the mean-only baseline
                  # to what this fixture supports. See the README for the
                  # numbers on both sides of it.
                  epochs: 150
                  lr: 0.05
                  decay: 0.99
                  lambda: 0.05
      worker1:
        instances:
          worker1:
            iproto:
              listen:
                - uri: '127.0.0.1:3302'
            roles: [pregel.roles.worker]
            roles_cfg: &worker_cfg
              pregel.roles.worker:
                name: mf
                app: app
                master: '127.0.0.1:3301'
                workers:
                  - '127.0.0.1:3302'
                  - '127.0.0.1:3303'
                  - '127.0.0.1:3304'
                user: pregel
                password: 'secret'
                # The same table as the master's: the paths are ignored here
                # (this app has no worker_preload) and the hyperparameters are
                # ignored there. One anchor beats two lists that can drift.
                app_cfg: *app_cfg
      worker2:
        instances:
          worker2:
            iproto:
              listen:
                - uri: '127.0.0.1:3303'
            roles: [pregel.roles.worker]
            roles_cfg: *worker_cfg
      worker3:
        instances:
          worker3:
            iproto:
              listen:
                - uri: '127.0.0.1:3304'
            roles: [pregel.roles.worker]
            roles_cfg: *worker_cfg
